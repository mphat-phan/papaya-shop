import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#E56717',
    },
    text:{
      primary: "#333333"
    }
  },
  header: {
    backgroundColor: '#FFFFFF',
    color: '#333333'
  }

});